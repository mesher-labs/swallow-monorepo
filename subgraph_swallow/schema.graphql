type ShortCut @entity {
  id: ID!
  name: String!
  endpoint: String!
  selector: String!
  contractAddr: Bytes!
  userParams: [UserParam!]! @derivedFrom(field: "shortcut")
  shortCutsType: String!
}

type UserParam @entity {
  id: ID!
  name: String!
  value: String!
  shortcut: ShortCut!
}
